void main {
    let float x;
    x := readIO();
    divisores(x);
}

def void divisores(float n) {
    let float i;
    i := 1.0;
    n := (n + 1.0);
    while (i <= n) do {
        if ((n % i) == 0.0) {
            printIO i;
        };
         i := (i + 1.0);
    };
}